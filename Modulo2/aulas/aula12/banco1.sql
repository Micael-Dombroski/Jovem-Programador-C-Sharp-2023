use Banco1;

/*create table Conta (
    Id int identity(1,1) not null,
    Agencia int not null,
    Numero int not null,
    Correntista varchar(255) not null,
    Saldo decimal(10,2)
);

alter table Conta
    add constraint UQ_Conta_Agencia unique (Agencia, Numero);

alter table Conta add constraint PK_Conta
    primary key clustered (id);*/

